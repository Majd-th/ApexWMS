<a href="/api/legends/manage" class="back-link">← Back to Legend Management</a>

<div class="edit-page">
  <h2>Edit Legend: <%= legend.name %></h2>

  <form
    action="/api/legends/<%= legend.legend_id %>/edit"
    method="POST"
    class="edit-form"
  >
    <!-- Name -->
    <div class="field">
      <label for="name">Legend Name</label>
      <input
        id="name"
        type="text"
        name="name"
        value="<%= legend.name %>"
        required
      />
    </div>

    <!-- Role -->
    <div class="field">
      <label for="role">Role</label>
      <input
        id="role"
        type="text"
        name="role"
        value="<%= legend.role %>"
        placeholder="Offense / Defense / Recon / Support"
        required
      />
    </div>

    <!-- Description -->
    <div class="field">
      <label for="description">Description</label>
      <textarea
        id="description"
        name="description"
        rows="4"
        required
      ><%= legend.description %></textarea>
    </div>

    <!-- Buttons -->
    <div class="buttons">
      <button type="submit" class="btn save">Save Changes</button>
      <a href="/api/legends/manage" class="btn cancel">Cancel</a>
    </div>
  </form>
</div>

<style>
  .back-link {
    display: inline-block;
    margin-bottom: 10px;
    color: #5b0bb5;
    text-decoration: none;
    font-size: 14px;
  }
  .back-link:hover {
    text-decoration: underline;
  }

  .edit-page {
    padding: 30px 35px 40px;
    border-radius: 12px;
    background-image: url("/img/legends.jpg"); /* same bg as manage */
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    box-shadow: 0 0 18px rgba(0, 0, 0, 0.7);
    max-width: 700px;
    margin: 0 auto;
  }

  .edit-page h2 {
    color: #fff;
    margin-bottom: 20px;
  }

  .edit-form {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .field {
    display: flex;
    flex-direction: column;
  }

  .field label {
    color: #fff;
    margin-bottom: 6px;
    font-weight: 600;
  }

  .field input,
  .field textarea {
    padding: 10px;
    border-radius: 6px;
    border: none;
    font-size: 14px;
  }

  .buttons {
    margin-top: 10px;
    display: flex;
    gap: 10px;
  }

  .btn {
    padding: 10px 18px;
    border-radius: 6px;
    font-weight: bold;
    text-decoration: none;
    border: none;
    cursor: pointer;
    font-size: 14px;
    transition: 0.2s;
    text-align: center;
  }

  .btn.save {
    background: #4caf50;
    color: #fff;
  }

  .btn.cancel {
    background: #555;
    color: #fff;
  }

  .btn:hover {
    transform: scale(1.05);
    filter: brightness(1.05);
  }
</style>
